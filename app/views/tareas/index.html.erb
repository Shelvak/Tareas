<h1>Listado de tareas</h1>

<% if params[:responsable_id] %>
<h4>(Ordenado por: 
<%=  Responsable.find_by_id(params[:responsable_id]) %> )
<% end %> </h4>
<table>
  <tr>
    <th>Nombre</th>
    <th>Descripción</th>
    <th>Autor</th>
    <th>Fecha</th>
    <th>Responsable</th>
  </tr>

<% @tareas.each do |tarea| %>

  <tr>
    <td><%= link_to tarea.nombre, tarea %></td>
    <td><%= truncate tarea.detalles, :length => 25 %></td>
    <td><%= link_to tarea.autor, tareas_path(:autor => tarea.autor) %></td>
    <td><%= l tarea.fecha %></td> 
    <td><%= link_to tarea.responsable, 
            tareas_path(:responsable_id => tarea.responsable_id) %> </td>
  </tr>
  
<% end %>
</table>
<br />

<%= link_to 'Nueva Tarea', new_tarea_path %> |
<%= link_to 'Nuevo Responsable', new_responsable_path %> <br />
<%= will_paginate @tareas,
  :previous_label => '&laquo; Anterior',
  :next_label => 'Siguiente &raquo;'  %>