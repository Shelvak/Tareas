<%= form_for(@tarea) do |f| %>
  <% if @tarea.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tarea.errors.count, "error") %> La tarea no se pudo guardar:</h2>

      <ul>
      <% @tarea.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br />
    <%= f.text_field :nombre, placeholder: 'Nombre...' %>
  </div>
  <div class="field">
  <%= f.label :detalles %><br />
  <%= f.text_area :detalles, :rows => 5, placeholder: 'Detalles...' %>
 </div>
 <div class="field">
  <%= f.label :autor %><br />
  <%= f.text_field :autor, placeholder: 'Quien lo debería hacer...' %>
 </div>
 <div class="field">
  <%= f.label :fecha %><br />
  <%= f.date_select :fecha, :order => [:day, :month, :year] %>
 </div> 
 <div class="field" > 
   <%= f.label :auto_responsable, 'Responsable' %>
   <%= f.text_field :auto_responsable, value: @tarea.responsable.try(:nombre) %>
  <%# f.label :responsable_id %><br />
  <%#= f.hidden_field :responsable_id, :value => @current_responsable.id %>
  <%# f.select :responsable_id, Responsable.all.map {
  |r| [r.nombre, r.id]}, :include_blank => true  %>
 </div>
  <div class="actions">
    <%= f.submit 'Guardar' %>
  </div>
<% end %>
