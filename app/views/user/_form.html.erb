<%= form_for(@responsable) do |f| %>
  <% if @responsable.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@responsable.errors.count, "error") %> prohibited this responsable from being saved:</h2>

      <ul>
      <% @responsable.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br />
    <%= f.text_area :nombre %>
  </div>
  <div class="field">
    <%= f.label :login, 'Mail' %><br />
    <%= f.text_area :login %>
  </div>
  <div>
   <%= f.label :password, 'Clave' %>
   <%= f.password_field :password, :size => 40 %>
  </div>
  <div>
   <%= f.label :password_confirmation, 'Respetir Clave' %>
   <%= f.password_field :password_confirmation, :size => 40 %>
  </div>
  <div class="actions">
    <%= f.submit 'Crear'%>
  </div>
<% end %>
