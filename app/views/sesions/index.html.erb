<h1>Listing sesions</h1>

<table>
  <tr>
    <th>User</th>
    <th>Password</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @sesions.each do |sesion| %>
  <tr>
    <td><%= sesion.user %></td>
    <td><%= sesion.password %></td>
    <td><%= link_to 'Show', sesion %></td>
    <td><%= link_to 'Edit', edit_sesion_path(sesion) %></td>
    <td><%= link_to 'Destroy', sesion, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Sesion', new_sesion_path %>
